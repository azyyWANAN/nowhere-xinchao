# 乌有乡明信片豪华版 · 升级指南（给姐姐）

> 适用：已部署乌有乡网页看板（nowhere_view.py + index.tpl.html）的环境。
> 先备份你的 web 目录！然后把本仓库的文件覆盖过去，按下面三步配置。

## 第一步：覆盖文件

把仓库里的以下内容复制到你的乌有乡项目：

- `web/nowhere_view.py`、`web/index.tpl.html`（覆盖）
- `web/city_abbr.py`、`web/imggen.py`（新增）
- `web/fonts/`（新增，Bodoni Moda + 思源宋体）
- `web/img_prompts.json`（新增，地点场景表，可改）
- `roadnet.py`（覆盖，含无路纹理逻辑）
- `textures/`（新增，12 张默认大地纹理，可用自己的图替换）

若你还没打 AIR MAIL 缩写补丁，跑一次 `airmail_fix.py`（或按 city_abbr.py + _dest_short 改法手工合入）。

## 第二步：配置

1. **页面钥匙**：启动时设环境变量 `NOWHERE_VIEW_KEY=你的钥匙`（不设则无鉴权，请自行评估）。
2. **生图**（可选）：
   - 不生成：什么都不用做；
   - 要生成：把 `nwimg_config.example.json` 复制为 `nwimg_config.json`，
     填 `"enabled": true` 和你的 MiniMax `api_key`（sk-cp 开头），约 0.025 元/张；
   - 高配：按示例在 `img_prompts.json` 给地点写场景句。
3. **音乐条**：在乌有乡数据目录（web 读取的那个目录）放 `music.json`，格式：
   `{"巴黎": {"title": "Clair de lune", "artist": "Debussy", "url": "/nwmusic/xx.mp3", "note": "一句话"}}`
   音频文件放同目录 `music/` 下。建议 90 秒、128kbps。
4. **城信**：可选，数据目录放 `city_letters_mine.json`：`{"地点": "一句话"}`，
   没写的城市自动回退到内置 30 条模板。

## 第三步：重启验证

重启 web 服务，刷新页面，检查：字体、明信片音乐条、AIR MAIL 缩写、地图视角折叠、实景相册、路网纹理。

## 常见问题

- 音乐条不显示？→ 该地点在 music.json 里没有 url（没配歌就不显示，正常）。
- 路网图空白/转圈？→ 已有多镜像重试；实在无路会画大地纹理；纹理目录缺失时退回代码画波纹。
- 生图跑偏（城市画成森林）？→ 通用提示词已加固；给该地点配 img_prompts 场景句即可。
- 字体没变化？→ 确认 web/fonts/ 存在，且页面能访问 /nwfonts/。
