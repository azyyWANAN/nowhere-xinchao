# 乌有乡旅程档案 · 小白部署指南

> 从零开始，把"AI 旅行 + 档案网页 + 路网 + 情书"整套跑起来。照着做，每一步都写了为什么。

---

## 一、你需要准备什么

- 一台 **Ubuntu 服务器**（国内云服务器即可，2核2G够用），能 SSH 上去；
- 一台**手机**（看档案网页）；
- 一个**能接 MCP 的 AI 前端**（比如 Operit、AstrBot、Claude 桌面版等，任选一个）；
- 十分钟耐心。

## 二、装乌有乡本体（AI 的"身体"）

乌有乡是原作者的仓库，我们不改它，只装：

```bash
cd ~/apps
git clone https://github.com/yuyixuanfu/nowhere.git
cd nowhere
python3 -m venv .venv
.venv/bin/pip install -e .
```

跑起来（这就是 MCP 服务）：

```bash
cd ~/apps/nowhere
.venv/bin/python3 bridge.py
```

它默认在 `0.0.0.0:8090` 提供 MCP 接口。前端 MCP 配置填：`http://你的服务器IP:8090/mcp`。

## 三、装档案网页（人看的页面）

本仓库（nowhere-xinchao）就是这一层：

```bash
cd ~/apps
git clone https://github.com/azyyWANAN/nowhere-xinchao.git
cd nowhere-xinchao/web
NOWHERE_HOME=/home/你的用户/.nowhere NOWHERE_VIEW_KEY=你自己设的钥匙 python3 nowhere_view.py
```

手机浏览器打开：`http://你的服务器IP:18082/?k=你自己设的钥匙`。

（如果图渲染需要高德地图：把高德 Web 服务 key 放进 `apps/nowhere/map_keys.env`，一行 `AMAP_KEY=你的key`。没有 key 也能用——高德画不到的地方会自动切 OpenStreetMap。）

## 四、让 AI 去旅行

在你的 AI 前端里，对乌有乡 MCP 说：

- "打开一扇门，去杭州"
- "往东走两公里"
- "寄一张明信片回家"

**寄明信片会触发三个自动小工**（服务器后台，不烧你的 AI）：

1. 明信片正面自动画成**真地图**（国内高德、海外 OSM）；
2. 自动把所在城市的**路网**描下来（Overpass 数据 + 本地画图）；
3. 自动截一段**本地电台**（20 秒，存服务器）。

## 五、随行耳朵（可选，让 AI 真听电台）

如果想让 AI 写下"他听见了……"，需要配一个多模态感官层（比如 cove-sensory）。没有也没关系：页面上会显示播放条，**人自己点开听**。

国内电台靠 `radio_ear.py` 里的 `CN_BOOK` 通讯录（蜻蜓FM 的 live 编号），国外靠 radio-browser.info 黄页。没有收录的台会兜底到远方电台——边远之地的浪漫。

## 六、哪些东西你可以改

| 想改什么 | 在哪改 |
| --- | --- |
| 网页访问钥匙 | 启动时 `NOWHERE_VIEW_KEY` |
| 明信片收信人称呼（默认"烟烟"） | 启动时 `USER_NAME=你的称呼` |
| 情话 | `love_letters.json`（30 句，{城} 和 {你} 是占位符，句子随便改） |
| 城市表 | `cn_cities.json`（337 城，可增删） |
| 国内电台 | `radio_ear.py` 的 `CN_BOOK`（蜻蜓FM 编号） |
| 页面配色、字号 | `web/index.tpl.html` 的 CSS 变量 |
| 路网颜色/纸底 | `roadnet.py` 里的 `WIDTHS` 与配色 |

## 七、常见问题

- **地图是白的？** 高德没画到（海外常见），会自动切 OSM；还白就刷新页面（图地址带防缓存参数）。
- **电台是外语？** 当地台没被黄页收录，通讯录里补一条，或当它是"飘洋过海的声音"。
- **海拔显示 300 米不对？** 那是乌有乡本体的默认假数据，网页已不再显示海拔，只看地表类型。
- **路网图没出现？** 等一分钟，Overpass 画图慢半拍；或手动跑 `roadnet.py 纬度 经度 名字`。

## 八、目录速查

```
nowhere-xinchao/
├── radio_ear.py        随行耳朵（找台/截流/记听后感）
├── roadnet.py          路网指纹（Overpass → 米色纸深棕线）
├── love_letters.json   情书库（30 句）
├── cn_cities.json      中国城市表（337 城）
├── web/
│   ├── nowhere_view.py 档案网页服务
│   └── index.tpl.html  页面模板与样式
├── fetch_cards.py      手机自动收明信片图
└── travel_archiver.py  旅行日记归档
```

祝你旅途愉快。走到哪，路都认得你。
